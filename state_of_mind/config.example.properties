# Copy this file to config.properties in the same folder and fill in your
# real values. config.properties itself is git-ignored and never gets
# pushed to GitHub — this .example file is the only one that's safe to commit.

tmdb.api.key=your_tmdb_api_key_here
db.url=jdbc:mysql://localhost:3306/state_of_mind
db.user=root
db.password=your_local_mysql_root_password_here
